server:
  port: 8085

spring:
  application:
    name: eureka-sleuth
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost

